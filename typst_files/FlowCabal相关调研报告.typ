#set text(font: ("Sarasa Fixed Slab SC"), lang:("zh"))
#show math.equation: set text(font: "Neo Euler")

最近看了些 Agent “最新” 设计的总结@agenttalks，其中提到的讨论对于 “shell命令+文件系统” 范式的 Agent 颇为认可，FlowCabal 也将采用这类设计。

但这些肯定是老生长谈的东西,实际上，这篇文章真正要说的就是开源社区中以此为理念的项目


#bibliography("ref.bib")